spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/etonote
    username: postgres
    password: tesla
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  port: 3001
  error:
    include-message: always